const state = {
  message: {},
}

const mutations = {
  SET_TOURIST(state, message) {
    state.message = message
  },
  SET_USER(state, message) {
    state.message = message
  },
}

export default {
  state,
  mutations,
}
